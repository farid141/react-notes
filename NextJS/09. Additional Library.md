# JS Web Package

## xss

untuk sanitize input data dari FE
`xss(data.description)`

## Sluggify

untuk membentuk slug (biasanya untuk title). Berguna untuk meningkatkan SEO sebagai dynamic path URL
`sluggify(data.title)`

## Storing Image

storing user uploaded photo:

- in db (as base64)
- in server folder
